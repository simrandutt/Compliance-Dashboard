import React from 'react';
import '../styles/dashboard.scss';

const ListView: React.FC = () => {
  return (
    <div className="page-container">
      <p>This is the List View page where more detailed lists can be displayed.</p>
    </div>
  );
};

export default ListView;
