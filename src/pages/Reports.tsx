import React from 'react';
import '../styles/dashboard.scss';

const Reports: React.FC = () => {
  return (
    <div className = "page-container">
      <p>This is the Reports page where reports and analytics can be shown.</p>
    </div>
  );
};

export default Reports;
